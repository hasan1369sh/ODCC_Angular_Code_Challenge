import { OnlyLetter } from './only-letter';

describe('OnlyLetter', () => {
  it('should create an instance', () => {
    const directive = new OnlyLetter();
    expect(directive).toBeTruthy();
  });
});
